"""Sandbox risk policies.

Each policy implements the ``RiskPolicy`` protocol defined in
``app.security.risk`` and contributes a list of ``RiskAssessment`` items
when asked to evaluate a command.
"""
