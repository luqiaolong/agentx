feat(team): T19+T30 DangerousTaskClassifier + 7 测试场景

T19 classifier.py:
- 新建 DangerousTaskClassifier 类（替代 planner._looks_like_dangerous_task）
- 优先级：缓存 → LLM 结构化输出 → 关键词降级
- LLM 路径用 with_structured_output(ClassificationResult)
- 关键词降级复用 utils.text.compile_keyword_patterns
- 按 description SHA256 hash 缓存，避免同 task 多次 LLM 调用
- _DANGEROUS_KEYWORDS 从 planner.py 迁移到 classifier.py 统一维护

T30 test_team_v2_classifier.py（7 场景）:
- 关键词降级：中文命中 / 英文 ASCII 词边界 / 安全任务不命中
- LLM 路径：结构化输出正常 / LLM 异常降级 / 不支持 structured output 降级
- 缓存：同 description 多次调用只触发一次 LLM

验证: ruff check 通过, 1457 单元测试通过（7 新增）。
