test(team): T22/T23/T37/T39 replan + warnings + routing 测试

新增 test_team_v2_replan.py（9 场景）：
- T39 _route_after_aggregate: 质量门通过→END / 失败→replan / 默认通过
- T22/T23 replan_node: replan_count 限制 / 无新任务路由 / 有新任务追加+waves
- T37 warnings channel: aggregate_node 发射 state.warnings 累积的 SSE
- _route_after_replan: 有 wave→dispatch / 无 wave→END

修复（events.py）：
- make_sse_event 的 JSON 序列化事件列表新增 "warning"
  （原 "warning" 事件走 default 分支 str(dict) 不可解析）

验证: ruff check 通过, 1466 单元测试通过。
